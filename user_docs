=====================================================================

How to use NMRcraft:

- before you can actually run the script you have to login to huggingface by typing `huggingface_cli login` and follow the prompt from there
- to run a training run, execute the command `train`

=====================================================================
